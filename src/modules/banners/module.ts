import { banners } from './module/store'
import { routes, children } from './module/routes'

export default {
  name: 'banners',
  store: banners,
  routes: routes,
  // childRoutes: children
}
